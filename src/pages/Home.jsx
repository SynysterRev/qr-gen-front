import { ArrowRight, Sparkles, Zap, Shield } from "lucide-react";

export default function Home() {
    return (
        <section>
            <div className="max-w-7xl mx-auto px-4">
                <div className="inline-flex items-center gap-2 rounded-full bg-primary/10 border border-primary">
                    <h1>wahou</h1>
                </div>
            </div>
        </section>
    );
}